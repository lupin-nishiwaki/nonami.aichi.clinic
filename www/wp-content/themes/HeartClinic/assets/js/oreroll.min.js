const oreroll = function () { const e = document.querySelectorAll(".oreroll"); const t = new IntersectionObserver(function (e) { for (let t = 0; t < e.length; t++) { const o = e[t]; o.boundingClientRect.y, o.rootBounds.y, o.isIntersecting ? (o.target.classList.add("is-orerollActive"), o.target.classList.remove("is-orerollOver", "is-orerollReady")) : (o.target.classList.remove("is-orerollActive"), o.boundingClientRect.y < o.rootBounds.y && o.target.classList.add("is-orerollOver")) } }, { root: null, rootMargin: "0% 0% -160px", threshold: [0, .5, 1] }); t.POLL_INTERVAL = 100; for (let o = 0; o < e.length; o++)e[o].classList.add("is-orerollReady"), t.observe(e[o]) }();
